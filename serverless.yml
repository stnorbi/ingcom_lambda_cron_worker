service: batch-submit

provider:
  name: aws
  runtime: python3.7
  role: arn:aws:iam::189416701607:role/service-role/lambdaBatchRole
  timeout: 60

functions:
  stateSubmit:
    handler: handler.ingcom_submit
    layers:
        - arn:aws:lambda:eu-central-1:189416701607:layer:serverless-etl-dev-python-requirements:1
package:
    individually: true
    exclude:
        - "input.json"
        - "*.env"
        - ".env"
        - "Pipfile"
        - "Pipfile.*"
        - ".python-version"
        - ".gitignore"